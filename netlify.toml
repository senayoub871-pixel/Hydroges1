[build]
  command = "pnpm install && pnpm --filter @workspace/docexchange run build"
  publish = "artifacts/docexchange/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
